/**
 * Точка входу клієнтського застосунку. Поки що — мінімальний плейсхолдер,
 * щоб Vite-білд успішно проходив. Реальний UI зʼявиться у feature/ui-components.
 */

const rootElement = document.getElementById("root");
if (rootElement) {
  rootElement.textContent = "Хрестики-нулики — UI готується...";
}

export {};
